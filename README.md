# Vibely Frontend

React frontend for the Vibely music app.

## Run

1. Install dependencies
   - `npm install`
2. Configure env
   - copy `.env.example` to `.env`
3. Start dev server
   - `npm run dev`

The app connects to backend at `http://localhost:3000` by default and sends cookies with requests.
